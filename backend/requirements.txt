# Core dependencies - v3 without heavy NLP (Railway image size limit)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Document processing
python-docx>=1.1.0
diff-match-patch>=20230430
python-Levenshtein>=0.25.0
numpy>=1.26.0

# NLP dependencies removed - image size exceeded Railway's 4GB limit
# spacy>=3.7.0  # ~1.5GB with model
# sentence-transformers>=2.2.0  # ~2GB with torch

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
